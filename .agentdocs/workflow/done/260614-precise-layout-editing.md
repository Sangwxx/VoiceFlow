# 精确布局与二次编辑

## 目标

- 用户明确指定左、右、上、下时保留相对位置，不被自动布局覆盖。
- 支持语音移动节点、修改编号节点文字等二次编辑。
- Agent 可以通过受校验的 `move_node` Operation 使用同一移动能力。
- 未指定精确位置时继续使用自动布局。

## 完成项

- [x] 增加 `move_node` Operation 与运行时校验。
- [x] 基础图形生成提取相对位置约束。
- [x] 增加本地节点移动和更宽松的文字修改语法。
- [x] Agent 协议支持移动节点并保留位置。
- [x] 通过完整质量检查。
