# 修复自由画布 SVG 导出

## 目标

- 修复自由画布导出的 SVG 在部分查看器中显示空白的问题。
- 自由画布 SVG 导出使用纯矢量 XML，不依赖 `foreignObject`、HTML 或页面 CSS。
- 保持专业图表现有导出行为不变。

## 实施阶段

- [x] 定位空白 SVG 原因。
- [x] 实现自由画布纯 SVG 序列化器。
- [x] 补充测试并验证真实下载文件。
- [x] 完成质量检查并推送功能分支。

## 验证结果

- 浏览器真实下载杯子 SVG，文件大小为 926 字节。
- 文件包含杯身、杯口、杯把和热气对应的标准 `<path>`、`<ellipse>` 图元，不包含 `foreignObject`。
- 64 个测试文件、420 项测试全部通过，`typecheck`、`lint`、构建和本次变更文件格式检查通过。
